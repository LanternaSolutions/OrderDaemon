- fix show details/show technical details mess - only one expandable section with deveoper relevant details and raw json if any for that component, and any business relevant value key pairs should be shown in the main section of the component. this is to ensure users truly get the best possible data at a glance. - also, the buttons arent expanding the sections at all, theres eems to be a bug of some kind, my guess is that it is about inverted logical states for expanding/hiding and clicking the button
- fix batch delete in log stream - the issue is probably due to a mismatch between what the ui is showing the user in 'grouped' or 'individual' views of the lgo stream and the batch delete logic
- fix status pills in timeline components (replace redundant order # pill)
- add dash-icons and colors to the new components similar to what we had in the legacy renderers system
- remove all unnecessary dev/debug events that would fill up the log stream with noise and dont conform to the grouped/individual or timeline based event timelines - only keep the sort that are very relevant to normal users such as '10 log entries batch deleted by admin <username>' 
- final translation strings cleanup for free plugin
- pass the wordpress plugin checker